# Supabase Configuration
SUPABASE_URL=https://ibrcdwgyocvqkogxhnqh.supabase.co
SUPABASE_KEY=your_supabase_service_role_key_here

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=True

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# AI Model Configuration
MODEL_NAME=sentence-transformers/all-MiniLM-L6-v2
MODEL_CACHE_DIR=./models

# Recommendation Settings
# MIN_CONFIDENCE_SCORE: Minimum similarity score (0.0-1.0) for course recommendations
#   0.3-0.4 = More recommendations, less relevant (may include unrelated courses)
#   0.5-0.6 = Balanced (recommended default)
#   0.7-0.8 = Fewer recommendations, highly relevant only
MIN_CONFIDENCE_SCORE=0.5

# MAX_RECOMMENDATIONS: Maximum number of courses to recommend per request
MAX_RECOMMENDATIONS=10
